# challenger-solarmarket

## Como utilizar

1. Clonar o repositório
2. Dentro da pasta api rodar o comando `npm install` e em seguida `npm run dev`
3. Dentro da pasta client rodar o comando `npm install` e em seguida `npm start`
